import React from "react";
import "./Title.css";

const Title = props => (
    <div className="titlebox">
        <h1>Dog-Meme Clicky Game!</h1>
        <p>Click a meme for earned points, but don't click the same meme twice!</p>
    </div>
);

export default Title;